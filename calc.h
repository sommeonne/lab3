#ifndef CALC_H
#define CALC_H

class Calculator {
public:
    /**
     * Calculates the sum of the first n elements of the infinite series.
     * 
     * @param n The number of terms to sum.
     * @param x The variable in the series.
     * @return The sum of the first n elements of the series.
     */
    double FuncA(int n, double x);  
};

#endif 
